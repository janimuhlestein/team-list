const { TestScheduler } = require('jest');
const Employee = require('../lib/Employee');

test('creates a Employee object', () => {
    employee = new Employee('Dale Maughan', 1);
    expect(employee.name).toBe
    
});